// src/config.js

// It is best practice to store API keys in environment variables for security.
// For this example, we are defining it here.
export const GOOGLE_AI_API_KEY = import.meta.env.VITE_GOOGLE_AI_API_KEY;